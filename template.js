const firstName = 'Al Amin';
const lastName = 'Khan';

console.log(`${firstName} ${lastName}`); // to use tempate method have to use caret `` quote.

const multiLine =
`Hey ${firstName} ${lastName}
Have a good day.
`
console.log(multiLine);
